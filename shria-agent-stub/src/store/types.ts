export type ChallengeState={currentDay:number;lastCompletedDay:number;lastSessionAt?:string};export type UserPrefs={timeOfDay?:string;liked?:string[]};export type Reminder={time:string;label:string};export type UserData={userId:string;challenge:ChallengeState;prefs:UserPrefs;reminders:Reminder[]};export type EventRecord={name:string;payload?:Record<string,any>;at:string};export interface Store{getUser(u:string):Promise<UserData>;upsertUser(u:UserData):Promise<void>;setChallengeDay(u:string,d:number):Promise<UserData>;addEvent(u:string,e:EventRecord):Promise<void>;setReminder(u:string,r:Reminder):Promise<void>;}
